# Matriz de impacto — quem depende de quem

**Dono único deste mapa:** este arquivo. O passo 2 (`/2-entender-e-criar-issue-base`, leitura 3) lê
daqui os **vizinhos a testar**; o passo 3 (Prova 2) roda os testes deles; o auditor (ângulo D)
confere. Módulo que não está aqui não tem vizinho declarado — e isso é uma afirmação que o
auditor pode refutar.

**Como preencher:** uma linha por módulo. "Vizinhos" = quem lê o mesmo dado ou chama a mesma
função. "Teste do vizinho" = o comando que fica vermelho se o vizinho regredir. **Crítico** =
mudança aqui exige companion obrigatório na prova (Prova 1) e o olho do dono no merge.

| módulo | caminho | vizinhos (leem o mesmo dado) | teste do vizinho | crítico? |
|---|---|---|---|---|
| _(exemplo)_ `pricing` | `src/pricing/` | `reservas`, `financeiro` | `node --test tests/reservas.test.mjs tests/financeiro.test.mjs` | sim |
| `trilha-e-privacidade` | `src/dominio/eventos_trilha.py`, `src/dominio/redator_pii.py`, `src/aplicacao/servico_trilha.py`, `src/infra/trilha_jsonl.py` | F3 (#6, grava `tentativa_de_cotacao`), F5 (#8, orquestra e grava a maioria dos eventos), F6 (#9, usa o redator na fronteira do LLM), F8 (#11, lê a trilha para `eval/casos.jsonl`) | `.venv/Scripts/python -m pytest tests/dominio tests/aplicacao tests/infra tests/integracao` (`PYTHONPATH=src`) | sim |
| `dominio` | `src/dominio/` | `aplicacao` (F3, #6 — ainda não existe), `infra`/`interfaces` (F4/F5, #7/#8) | `pytest tests/dominio/` + `pytest tests/arquitetura/test_fronteiras.py` | sim |
| `aplicacao.portas.portal_de_cotacao` / `infra.cliente_quote` | `src/aplicacao/portas/portal_de_cotacao.py`, `src/infra/cliente_quote.py` | `dominio.resultado_cotacao`/`dominio.preco_cotado` (consome, não replica — LEI 11); `aplicacao.servico_conversa`/`interfaces.cli` (consomem a porta) | `pytest tests/infra/test_cliente_quote.py tests/aplicacao/ tests/interfaces/ tests/dominio/` + `pytest tests/arquitetura/test_fronteiras.py` | sim |
| `aplicacao.servico_conversa` / `interfaces.cli` | `src/aplicacao/servico_conversa.py`, `src/interfaces/cli.py` | `dominio.politica`/`dominio.redator`/`dominio.validacao` (decide e escreve, nunca reimplementado aqui); `aplicacao.portas.portal_de_cotacao` (só a porta, nunca `infra` direto de `aplicacao`); `aplicacao.servico_trilha`/`dominio.eventos_trilha` (F4/#7 — grava a trilha, nunca chama `RepositorioDeTrilha` direto nem redige PII por conta própria); `interfaces.chat_mensagem` (issue #51, parte 2 — `POST /api/chat/mensagem`, roteada por `interfaces.servidor`, chama as MESMAS `registrar_pergunta_de_coleta`/`registrar_resposta_de_coleta`, dono único da escrita da trilha, LEI 11); `interfaces.http_comum` (extraído de `servidor.py` pelo teto do `file-loc-ceiling`, issue #51 — `json_resposta`/`ler_corpo_json`/`conversation_id_ou_400`, dono único das bordas HTTP genéricas; o PR #75/chat 12 reaproveita depois deste merge) | `pytest tests/aplicacao/ tests/interfaces/test_cli.py tests/dominio/` + `pytest tests/arquitetura/test_fronteiras.py` | sim |
| `conhecimento` (`RepositorioDeConhecimento`, `RepositorioDeConfiguracaoComercial`) | `src/dominio/ficha_objecao.py`, `src/aplicacao/portas/repositorio_conhecimento.py`, `src/aplicacao/portas/repositorio_configuracao_comercial.py`, `src/aplicacao/servico_conhecimento.py`, `src/aplicacao/servico_configuracao_comercial.py`, `src/infra/repositorio_conhecimento_json.py`, `src/infra/repositorio_configuracao_comercial_json.py`, `src/infra/planos_http.py` (`ids_dos_planos`), `src/interfaces/servidor.py`, `src/interfaces/cli.py` | F13/#43; `dominio.configuracao_comercial.ConfiguracaoComercial`/`dominio.politica.decidir` (consome, não replica — LEI 11, #42); `infra.planos_http.buscar_planos` (a `/planos`, para o vocabulário de marcadores por plano — B2 da auditoria do PR #45); futuro (fora de escopo da #43): frente que liga o chat ao agente real, frente do LLM lendo a base de conhecimento | `pytest tests/dominio/test_ficha_objecao.py tests/aplicacao/test_servico_conhecimento.py tests/aplicacao/test_servico_configuracao_comercial.py tests/infra/test_repositorio_conhecimento_json.py tests/infra/test_repositorio_configuracao_comercial_json.py tests/interfaces/test_servidor.py tests/interfaces/test_cli.py` + `pytest tests/arquitetura/test_fronteiras.py` | sim |
| `interfaces.painel` (`tela_fila_humana`, `tela_regras`) | `src/interfaces/painel/tela_fila_humana.py`, `src/interfaces/painel/tela_regras.py` | `dominio.decisao.MotivoHandoff` (lê o Enum inteiro, nunca redigita valor) — motivo novo no Enum exige entrada em `tela_fila_humana._DESCRICAO_MOTIVO`, senão cai no fallback "sem descrição registrada". Issue #51/#55: `tela_regras` recebe `infra.planos_http.buscar_planos`/as constantes de `infra.cliente_quote` só através do chamador (`painel/gerar.py`), nunca importando `infra` direto — a fronteira nova é `interfaces.CONTRACT.md` I-4. | `pytest tests/interfaces/painel/test_tela_fila_humana.py tests/interfaces/painel/test_tela_regras.py` | não |
| `interfaces.painel` (`layout`, `gerar`) | `src/interfaces/painel/layout.py`, `src/interfaces/painel/gerar.py` | as seis `tela_*.py` do painel (`tela_conversas`, `tela_fila_humana`, `tela_rastreio`, `tela_cotacoes`, `tela_avaliacao`, `tela_regras` — todas chamam `layout.pagina`/`pagina_ativa`, achado da #46: a casca em si não tinha linha própria na matriz); `interfaces.servidor` (issue #46 — `/`, `/conhecimento` e o chat montam a página via `layout.pagina`/`interfaces.chat.tela_chat`, mesma casca do painel; `gerar_paineis` chamada de novo a cada `/api/chat/cotar`/`/api/chat/contratar`, ADR-0005) | `pytest tests/interfaces/painel/ tests/interfaces/test_servidor.py tests/interfaces/chat/` | não |
| `contato` (`ContatoLead`, `RepositorioDeContato`) | `src/dominio/contato_lead.py`, `src/aplicacao/portas/repositorio_contato.py`, `src/aplicacao/servico_contato.py`, `src/infra/repositorio_contato_json.py`, `src/interfaces/painel/tela_fila_humana.py` (mostra nome/WhatsApp no card), `src/interfaces/painel/gerar.py` (monta `contatos` para a Fila humana), `src/interfaces/servidor.py` (`POST /api/chat/contato`, único caminho de escrita) | `dominio.eventos_trilha`/`aplicacao.servico_trilha` — nunca escreve nome/WhatsApp/e-mail lá (ADR-0005, `docs/PRIVACIDADE.md`); `interfaces.chat` (chama `POST /api/chat/contato` assim que nome+WhatsApp existem, antes do fim do fluxo) | `pytest tests/dominio/test_contato_lead.py tests/aplicacao/test_servico_contato.py tests/infra/test_repositorio_contato_json.py tests/interfaces/painel/test_tela_fila_humana.py tests/interfaces/painel/test_gerar.py tests/interfaces/test_servidor.py` | sim (PII fora do git) |
| `interfaces.chat` | `src/interfaces/chat/tela_chat.py`, `src/interfaces/chat/_corpo.html` | `interfaces.painel.layout` (casca compartilhada, `layout.pagina`); `interfaces.servidor` (rotas `/`, `/api/planos`, `/docs/design/paises.json`, `/api/chat/*`); `aplicacao.servico_conversa` (o agente real por trás de `/api/chat/cotar`/`/api/chat/contratar`, nunca reimplementado no JS) | `pytest tests/interfaces/chat/ tests/interfaces/test_servidor.py` | não |
| `resposta-orientada` (issue #58) | `src/dominio/ficha_objecao.py` (`preencher_marcadores`), `src/aplicacao/portas/portal_de_resposta_orientada.py`, `src/aplicacao/servico_resposta_orientada.py`, `src/infra/adaptador_de_linguagem.py` (`AdaptadorDeRespostaOrientada*`), `src/interfaces/rotas_resposta_orientada.py` | `dominio.intencao.Intencao`/`dominio.decisao.MotivoHandoff` (novos membros, aditivos — LEI 11, nunca redigitados); `aplicacao.servico_conversa` (`intencao_reconhecida`/`_TEXTO_ENCAMINHAMENTO_PARA_CORRETOR`, reaproveitados, não duplicados); `interfaces.servidor` (`_rotear` chama `interfaces.rotas_resposta_orientada`, fora de `_rotear_chat` de propósito); `interfaces.painel.tela_fila_humana` (`_DESCRICAO_MOTIVO` ganha `RESPOSTA_ORIENTADA_INDISPONIVEL`); `interfaces.chat._corpo.html` (`habilitarCampoDeObjecao`) | `pytest tests/dominio/test_ficha_objecao.py tests/aplicacao/test_servico_resposta_orientada.py tests/infra/test_adaptador_de_resposta_orientada.py tests/interfaces/test_rotas_resposta_orientada.py tests/interfaces/painel/test_tela_fila_humana.py tests/interfaces/chat/` | sim (nunca fabrica número, nunca vaza PII pro LLM) |
| `interfaces.painel` (`tela_relatorio`, issue #59, PR 1/2) | `src/interfaces/painel/tela_relatorio.py` | Nenhum chamador de produção ainda (arquivo novo, não importado por `gerar.py` nesta versão — a ligação real e o item de menu em `layout.py` ficam para o PR 2/2, depois do merge da #57 PR 2). Vizinho futuro declarado: `dominio.status_conversa.StatusDaConversa` (#57 PR 2/2) — quando mergear, `render()`/`gerar_csv()` continuam recebendo status como string pronta (nunca importam o Enum direto, LEI 11); o fixture provisório de 5 valores em `test_tela_relatorio.py` é substituído pelos valores reais nesse PR. | `pytest tests/interfaces/painel/test_tela_relatorio.py` | sim (nome do lead: CSV nunca leva o nome completo nem WhatsApp/e-mail) |
| `atendimento` (issue #86, PR a — servidor e aplicação, EM ANDAMENTO) | `src/aplicacao/servico_atendimento.py`, `src/dominio/eventos_trilha.py` (`MensagemEnviada.sender_role`, I-17) | `dominio.status_conversa.StatusDaConversa`/`resposta_automatica_permitida` (#57 PR 2/2, ainda não em `main`) — até o merge, `status_atual`/`resposta_automatica_permitida` chegam por PARÂMETRO em `servico_atendimento`, nunca importados; troca de uma linha depois. `aplicacao.servico_resposta_orientada` (marca `sender_role="ia"`/`"agente"` em `MensagemEnviada`, LEI 11 — mesmo campo, um dono só grava). `interfaces/rotas_atendimento.py` (ainda não criado — entra depois do merge da #57, junto com a ligação real). | `pytest tests/dominio/test_eventos_trilha.py tests/aplicacao/test_servico_atendimento.py tests/aplicacao/test_servico_resposta_orientada.py` | sim (regra de silêncio: nenhuma resposta automática sai durante atendimento humano) |
|  |  |  |  |  |

## Regras

- **Erro se concentra nas BORDAS.** A refatoração mudou o dono do arquivo — o dono novo passou a
  testar? A migration mudou a coluna — quem lê passou a tratar o novo shape? Onde a informação
  atravessa uma borda, é onde ela cai. Esta matriz existe para nomear as bordas.
- **Dois lugares que decidem o mesmo valor = dois donos (LEI 11).** Ache-os no passo 2 e deixe um.
- Atualize esta matriz **no mesmo PR** que cria uma dependência nova. Matriz velha é pior que
  nenhuma: ela afirma que não há vizinho.
