# Changelog

Todo PR vencido que muda código deixa uma entrada aqui, citando a issue/PR (`#N`) — é assim que cada PR
fica mapeado e documentado (cobrado pelo guard `changelog-update`). Formato: Keep a Changelog.
Categorias: Adicionado · Alterado · Corrigido · Removido · Segurança.

## [Unreleased]

### Adicionado
- `src/interfaces/painel/`: gerador das seis telas do painel em HTML estático a partir da trilha real — `campos.py` (escape + buraco visível, as duas regras não-negociáveis do escopo), `agrupar.py` (agrupamento puro por conversa, sem regra de negócio), `layout.py` (casca com o `ui.css` real embutido, lido do disco), `tela_rastreio.py`/`tela_conversas.py`/`tela_fila_humana.py`/`tela_cotacoes.py`/`tela_regras.py`/`tela_avaliacao.py`. Sem JavaScript: proveniência abre em `<details>` nativo, cada conversa é uma seção ancorada — evita uma segunda regra de escape (contexto JS) que um `<script>const X=...</script>` exigiria. `tela_regras` lê `MotivoHandoff` do domínio (3 valores reais, não os 8 do mock) e mostra buraco visível para a política de retry até `src/infra/cliente_quote.py` existir (PR #35). `tela_avaliacao` mostra buraco porque `eval/casos.jsonl` é da F8/#11. Varredura de PII no HTML gerado reaproveita `dominio.redator_pii._PADROES`, nenhuma regex nova. 46 testes próprios, incluindo a "Regra de Regras" (lista de `reason_code` exibida == `{m.value for m in MotivoHandoff}`) e a prova de que `http_status=0` (timeout) nunca aparece cru. `examples/painel/` com trilha real fica pendente do merge do PR #35 (decisão da coordenação, issue #16) (#13)
- `src/infra/planos_http.py`: cliente só-leitura do `GET /planos`, com timeout explícito de 2s (testado que o timeout é passado) e `QUOTE_SERVICE_URL` lida do ambiente com o MESMO nome e default que `src/interfaces/cli.py:118` do PR #35 usa para a `/quote` (achado da coordenação, LEI 11: os dois clientes HTTP são borda e podem viver separados, mas a variável de configuração não pode divergir). Nasceu em `interfaces/painel/` por engano (HTTP é `infra`, âncora #3 §4) e foi movido antes do primeiro commit (#13)
- `infra/trilha_jsonl.py`: `todos_os_eventos()` em `RepositorioDeTrilhaJSONL` e `RepositorioDeTrilhaMemoria` — o painel (F10, issue #13) precisa enumerar todas as conversas de um arquivo de trilha sem conhecer os ids de antemão, e `eventos_da_conversa` exige o id. Vermelho antes por assertiva (`AttributeError`, três testes), verde depois; mesma interface nos dois adaptadores (âncora #3 §4) (#13)
- `src/dominio/CONTRACT.md`: seção da F4 por append no fim (R2 em #16, agora que a F2/#5 mergeou) — invariantes I-5/I-6/I-7 dos eventos da trilha e do redator de PII, sem editar nenhuma linha da seção da F2 (#7)
- `dominio/redator_pii.py`: mascaramento de PII (CPF, CEP, telefone, e-mail, placa Mercosul, nome próprio por lista conhecida) casando por FORMATO do valor, nunca pela palavra-rótulo — o `.capitalize()` de `scripts/generate_dataset.py:113` baixa `CPF`/`CEP` para minúsculo quando não são a primeira palavra do bloco (medido ao vivo no PLANO da #7), o que quebraria uma extração ancorada em palavra; `(?i)` é load-bearing na placa (char class `[a-z]` contra placa maiúscula do dataset) e redundante nos padrões só-dígito (CPF/CEP/telefone), distinção provada em teste, não só narrada. Vermelho antes por assertiva (esqueleto que devolvia o texto intacto), verde depois (#7)
- `dominio/eventos_trilha.py`: os 7 eventos da trilha auditável (`mensagem_recebida`, `mensagem_enviada`, `tentativa_de_cotacao`, `decisao`, `handoff`, `erro_marcado`, `correcao_registrada`) como dataclasses puras, com os campos de proveniência (`decisao_id`, `regra_aplicada`, `origem_do_texto`, `dados_usados`, `quote_attempt_id`) exigidos pelo acréscimo de escopo da coordenação; `reason_code` do `handoff` é `str` (o `.value` do `MotivoHandoff` que a F2/#5 vai possuir — R5 em #16), nunca o Enum importado (#7)
- `aplicacao/portas/repositorio_de_trilha.py`: porta `RepositorioDeTrilha` (`Protocol`) — pacote `aplicacao/portas/`, um arquivo por porta (decisão R3 em #16, para não colidir com a F3 declarando `PortalDeCotacao` em paralelo) (#7)
- `aplicacao/servico_trilha.py`: `ServicoDeTrilha`, a única porta de entrada para gravar um evento na trilha — redige todo campo textual (recursivo em dict/list/tuple) antes de delegar ao repositório, para nenhum caminho de escrita escapar do redator. Vermelho antes por assertiva (esqueleto que delegava sem redigir, CPF chegando ao repositório espião), verde depois (#7)
- `infra/trilha_jsonl.py`: `RepositorioDeTrilhaJSONL` (append-only, sem banco) e `RepositorioDeTrilhaMemoria` (dublê determinístico) implementando a porta `RepositorioDeTrilha`. Vermelho antes por assertiva (esqueleto abria em modo `"w"` e sobrescrevia a trilha a cada registro), verde depois com modo `"a"` (#7)
- `infra/exportador_trilha.py`: `exportar_execucao`, transforma a trilha de uma conversa no log de execução legível (entregável obrigatório do desafio) — lê pela porta, nunca do arquivo JSONL direto (#7)
- `tests/integracao/test_adaptador_espiao.py`: o teste exigido pela auditoria externa (ai-logs/codex/2026-09-11, incorporado na âncora #3 §9) — um `PortalDeLinguagem` espião prova que uma mensagem com CPF+telefone+e-mail+placa+CEP só chega mascarada ao LLM, e nem a trilha nem a resposta final guardam o valor original (#7)
- `tests/integracao/test_fidelidade_trilha.py`: o teste de fidelidade do acréscimo de escopo da coordenação — lê a trilha de uma execução completa e afirma que todo campo de proveniência da `ESPECIFICACAO.md` existe e está preenchido, e que mensagem com valor monetário tem `quote_attempt_id` de uma cotação com sucesso correspondente (#7)
- `src/aplicacao/CONTRACT.md` e `src/infra/CONTRACT.md`: primeiro contrato das duas camadas (R2 em #16) — `src/dominio/CONTRACT.md` fica para a F2/#5, dona da camada; a seção desta frente entra lá por append, no rebase pós-merge da F2 (#7)
- `docs/PRIVACIDADE.md`: decisão de não pedir CPF, o que é mascarado e onde, por que `(?i)` é a invariante (não estilo), e o limite conhecido e declarado da varredura por regex — README recebe só uma linha de ponteiro (R7 em #16) (#7)
- `governance/IMPACT_MATRIX.md`: linha do módulo `trilha-e-privacidade`, com os vizinhos futuros (F3/#6, F5/#8, F6/#9, F8/#11) que vão gravar ou ler eventos desta trilha (#7)
- `src/dominio/`: esqueleto do domínio puro (`EstadoDaConversa`, `ResultadoDaCotacao`, `PrecoCotado`, `Decisao`/`MotivoHandoff`, `politica.decidir`, `validacao` de formato, `redator.montar_mensagem`) e `tests/dominio/` com a bateria de testes das invariantes — commit 1/2 (esqueleto permissivo, vermelho por assertiva conforme o veredito de auditoria do plano; a invariante real entra no commit seguinte) (#5)
- `src/dominio/`: implementadas as 4 invariantes do domínio (I-1 a I-4, `src/dominio/CONTRACT.md`) — `PrecoCotado` só nasce de resposta com os campos de uma cotação bem-sucedida (`ValueError` senão), `Decisao(ENCAMINHAR)` sempre com `reason_code` e nenhum outro tipo aceita `reason_code`, `ResultadoDaCotacao` com `preco`/`motivo` mutuamente exclusivos, `redator.montar_mensagem` recusa qualquer coisa que não seja `PrecoCotado`; `politica.decidir` com a tabela de casos completa (R5 do #16: `erro_de_payload`/`timeout` viram `ENCAMINHAR`); `validacao` com formato real de CEP/data ISO — commit 2/2 (#5)
- `governance/IMPACT_MATRIX.md`: nova linha para o módulo `dominio` (#5)
- `pyproject.toml`: `pythonpath = ["src"]` em `[tool.pytest.ini_options]` — achado da F4 (#7): sem isto o pytest do guard (`python-check.mjs`, `companion-red-green.mjs`) não resolve `import dominio.x`/`aplicacao.x`/`infra.x`, e todo teste que importa um módulo real de `src/` falha na coleta. Linha copiada verbatim da F4, que é a dona (R9, #16); esta frente só herdou por precisar dela para rodar (#5, achado original #7). `tests/dominio/conftest.py` (a solução local que eu tinha usado antes desta linha existir) fica como dead code documentado — apagá-la agora reprova o `companion-red-green` (achado desta frente, comentado na #5: `add` num commit já feito + `delete` no índice do commit seguinte, na mesma branch ainda não mergeada, deixa o caminho na lista de "testes tocados" sem existir no working tree); remoção fica para depois do merge (#5)
- Mock: laço de aperfeiçoamento do agente — no Rastreio, cada resposta do agente pode ser **marcada como erro** e abre a sua **proveniência** (decisão tomada, regra aplicada, de onde veio o texto, que dados usou); a correção registrada vira **caso rotulado** na régua da tela Avaliação, onde reprova se o agente repetir o erro. Conceito inspirado no ciclo de curadoria de um agente já em produção do dono (lacuna vira pendência com status), adaptado ao nosso domínio — nenhum código de terceiros foi trazido (#18)
- Mock de design do console do agente em `docs/design/` — seis telas navegáveis por menu lateral (Conversas, Fila humana, Rastreio, Cotações, Avaliação, Regras e política), HTML estático sem dependência externa, com dados fictícios e a identidade visual derivada da marca do cliente. Serve para decidir o desenho antes de existir código de interface — e define, em particular, quais campos a trilha precisa gravar (requisito da F4, issue #7) (#18)
- `pyproject.toml` (ruff + pytest + `[dependency-groups] dev`) e `.importlinter` (contrato de camadas `dominio`/`aplicacao`/`infra`/`interfaces`): a máquina volta a medir o nosso código Python — 4 grupos de regras do `ruff` substituem os guards `empty-catch`, `catch-silent-blocker`, `await-unhandled` e `todo-debt-ratchet`, cegos em Python; `import-linter` substitui `import-boundaries`. `pylint`/duplicação e o guard de teste-companheiro ficam fora desta frente por decisão de escopo já registrada na issue #4 (#4)
- `tests/arquitetura/`: `test_escopo_do_lint.py` (o teste que pega a regressão desta frente — vermelho sem `pyproject.toml`, verde com ele), `test_fronteiras.py` (roda `lint-imports`) e `test_ruff_morde_fixtures.py`, com uma fixture ruim por regra em `tests/arquitetura/fixtures/` (#4)
- Esqueleto de 4 camadas em `src/` (`dominio/`, `aplicacao/`, `infra/`, `interfaces/`), pacotes vazios que dão à F2 em diante onde crescer e ao `import-linter`/ao teste de escopo algo real para medir (#4)
- ADR-0001 (`governance/adr/0001-guards-python-substitutos.md`) registrando a decisão acima (#4)
- Esteira de produção instalada pelo kit: CLAUDE.md com as 12 leis, governança (ADR com reserva de número, matriz de impacto, contrato de veredito, documentos obrigatórios), 63 scripts de guards em `scripts/esteira/`, pre-commit ligado, `esteira.json` (stack python, 7 áreas, pastas privadas declaradas proibidas) e `package.json` com os comandos da esteira (#3)
- Planejamento do projeto em `docs/`: proposta de design auditada externamente e plano da primeira frente; parecer da auditoria externa arquivado em `ai-logs/codex/` (#3)
- Pasta `_local/` ignorada pelo git, para chaves, sondas e rascunhos, com o critério escrito em `_local/LEIA-ME.md` (#3)

### Corrigido
- **Narrativa errada sobre `(?i)` no redator de PII (auto-achado, revisão do próprio PLANO da F4/#7):** o texto original dizia que CPF/CEP vazariam sem `(?i)` por causa do `.capitalize()` do gerador do dataset. Medido e refutado: `redigir_texto` nunca ancora na palavra "CPF"/"CEP", só no formato do valor (dígitos e separadores, sem letra) — removendo `(?i)` só do padrão de CPF, os 11 testes de `test_redator_pii.py` continuam verdes. Quem de fato precisa da flag é a placa (`[a-z]{3}...` só bate maiúsculo com a flag ligada) e o nome próprio (`re.IGNORECASE` explícito). Fato do `.capitalize()` continua real e medido; a causa-e-efeito com `(?i)` estava errada. Corrigido no docstring, em `docs/PRIVACIDADE.md` e com dois testes novos que provam a distinção em vez de narrá-la (#7)
- **`pyproject.toml` sem `pythonpath = ["src"]` (achado ao vivo na F4, issue #7):** nenhum teste que importasse um módulo real de `dominio`/`aplicacao`/`infra` conseguia coletar sob `python-check.mjs` ou `companion-red-green.mjs` — os dois rodam `pytest` sem setar `PYTHONPATH`, diferente de `tests/arquitetura/test_fronteiras.py`, que seta a variável à mão só para o subprocesso do `lint-imports`. F1/#4 nunca bateu nisso porque as quatro camadas eram só `__init__.py` vazio. Sem o achado, `companion-red-green` reprovava com `VERMELHO_NO_HEAD` mesmo com a implementação correta — ratchet, não regressão: nada rodava importando essas camadas antes desta frente (#7)
- **`resolverPython()` nunca tentava `python3` (causa 1, issue #27):** `python3` é o único binário que existe em container Debian/Ubuntu (`python`/`py` não existem lá) — sem ele na lista de candidatos, `companion-red-green` e `python-check` nunca achavam Python fora do Windows; conserto de produção, não só de self-test. Entra ANTES de `python` (inequívoco vs. nome historicamente ambíguo), depois do `.venv` (que continua com prioridade absoluta)
- **Três self-tests de guard presos à sintaxe do Windows (causa 2, issue #27):** `import-boundaries` (IB-3, caixa trocada dependendo do NTFS), `leis-integrity` (raiz fake `'C:\raiz'`) e `reserva-de-numero` (caminho absoluto `'C:/nao/existe/xyz'`, que `path.isAbsolute()` real não reconhece fora do Windows) só passavam no Windows — a produção já era portável nos três, só a FIXTURE do teste não era. IB-3 passa a sondar em runtime se o filesystem ignora caixa (nunca por `process.platform`) e sempre conta um caso, nas duas respostas — sonda errada vira contagem visivelmente errada, nunca caso desaparecido
- **`run-selftests.mjs` escondia o motivo do guard reprovado (causa 3, issue #27):** descartava `stdout`/`stderr` do guard filho, então o log do CI só tinha o resumo ("exit 1") — reproduzir em container era a única forma de descobrir a causa real. Agora imprime a saída (mascarando `SELF-TEST OK` como `SELF-TEST OK[eco]` ao ecoar, pra nunca se confundir com o veredito real de `julgarSaida`)
- **`companion-red-green.mjs` não funcionava fora do Windows (achado da auditoria fria, issue #27):** o link do `.venv` e do `node_modules` para dentro da réplica usava `execFileSync('cmd', ['/c', 'mklink', '/J', ...])`, que não existe em Linux/macOS — o `catch` engolia o `ENOENT` (fallback correto), mas o caso de self-test que afirma a junção acontecendo nunca tinha sido provado fora do Windows, e falhava lá. Trocado por `fs.symlinkSync(alvo, destino, 'junction')` nos dois pontos — junção no Windows, link simbólico no POSIX, sem depender de `cmd`. Reproduzido e confirmado em `node:24-bookworm` via Docker: o caso passa a funcionar nos dois sistemas (#4)
- **Dado pessoal em arquivo versionado (achado da auditoria fria, #17):** o `esteira.json` gravava o nome real do dono dentro de um caminho do campo `pastas_proibidas`, num repositório público. O campo inteiro saiu: era configuração de máquina, não pertence à entrega, e nada neste repositório o lia — a proteção que ele prometia era decorativa (#17)
- **Rastreabilidade dos arquivos vindos do kit (achado da auditoria fria, #17):** comentários nos 63 arquivos de `scripts/esteira/` citam números de issue e ADR do repositório de ORIGEM, que aqui significam outra coisa. Criado `scripts/esteira/README.md` declarando a proveniência, as quatro adaptações locais com o motivo, o limite conhecido dos 8 guards que não medem este diretório, e a regra para quem mexer (#17)

### Alterado
- `scripts/esteira/guards/companion-red-green.mjs`: a réplica descartável que o guard cria na base (para provar vermelho×verde) agora recebe um link (`fs.symlinkSync(..., 'junction')`) para o `.venv` do repositório real, no mesmo padrão que já existia para `node_modules`. Sem isso, `resolverPython(replica)` nunca achava o `.venv` (gitignored, ausente numa worktree recém-criada via `git worktree add`) e caía pro Python do sistema — que por decisão do dono não tem ruff/pytest instalados —, então **todo** PR Python com teste+fonte no diff saía `FERRAMENTA_AUSENTE` sempre, mesmo com o ambiente real funcionando. Achado e consertado durante a F1 (#4), com aval da coordenação; limite declarado: a réplica passa a compartilhar o mesmo `.venv` do repositório, não um isolado — aceitável porque o teste só lê o venv
- **Extensão de escopo (#4, com aval da coordenação):** o ambiente Python do CI (`.github/workflows/esteira.yml`) não instalava o que os testes de arquitetura desta frente precisam. Dois jobs tocados, mesmo motivo: `companion-red-green` não instalava nada (só `setup-python`) e `python-check` instalava `ruff pytest` sem `import-linter` — os dois rodam `pytest -q` sobre `tests/`, que agora inclui testes que chamam `lint-imports` via subprocess. Os dois passam a instalar `ruff pytest import-linter`, espelhando o que já existia; nada mais mudou nesses jobs. Mesmo conjunto (+`pylint`) declarado em `pyproject.toml` (`[dependency-groups] dev`), instalável com `uv sync --group dev`
- `.gitignore`: ignora `_local/`, `_PRIVADO/` e `*.token`; e abre duas exceções conscientes, porque as regras herdadas engoliriam entregáveis do desafio — `.env.example` (documenta variáveis sem segredo) e `examples/*.log` (o log da execução completa) (#3)
- `scripts/esteira/guards/companion-red-green.mjs`: os testes do diff passam a ser roteados **por extensão** — `.py` vai para o `pytest`, o resto vai para o `node --test` —, em vez de mandar tudo para o pytest só porque o projeto é Python. Antes, o teste Node que a própria esteira traz (`tests/esteira.test.mjs`) era entregue ao pytest, que coletava zero testes e saía 4; o guard traduzia isso como "pytest não instalado" e mandava instalar uma ferramenta que não faltava. Teste que não é `.py` no mesmo diff continua rodando: os dois resultados contam (#3)
- `scripts/esteira/lib/varredura.mjs`: o varredor passa a pular `.venv`, `venv`, `.uv`, `__pycache__` e `site-packages`, como já pulava `node_modules`. Sem isso, o `file-loc-ceiling` media bibliotecas de terceiros do Python e reprovava o commit por arquivos que não são nossos e que o `.gitignore` já exclui (#3)
- `CLAUDE.md` e as duas bibliotecas copiadas do kit (`scripts/esteira/lib/esteira.mjs` e `Esteira.ps1`): removida a menção a outro projeto, já que este repositório é público. No lugar da checagem por nome, ficou a equivalente estrutural — o caminho tem de terminar no slug deste projeto —, com o motivo escrito no próprio arquivo (#3)
